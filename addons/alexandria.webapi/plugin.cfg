[plugin]

name="Alexandria WebAPI"
description="HTTP endpoint API extension for the Alexandria Database library set.

Enables the querying and updating of the Alexandria database via HTTP API endpoints, creation and management of API keys and tokens, and restricting different API endpoints to different API keys or key categories."
author="Bryce Dixon"
version="1.1"
script="plugin.gd"
